//'use strict';
//import getList from './modulo.js';

const d = document,
  $root = d.getElementById('root'),
  devs = getList();
//console.log(devs);
let html = '';

/* for (const dev of devs) {
  console.log(dev);
} */
